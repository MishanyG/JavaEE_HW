
# 1. creating datasource section in standalone.xml config
data-source add \
   --jndi-name=java:/jboss/datasources/DS \
   --name=SQLDS \
   --connection-url=jdbc:mysql://localhost:3306/shop_ee \
    --driver-name=mysql \
   --user-name=root \
   --password=mikha

# 2. restarting Wildfly server
    :reload
